extends layout.pug

block content

  nav.nav
    a(href='/')
      span &larr;
      |  Back

  article.portfolio-projects
    .inner-wrapper.flex-row-wrap.two-col
      .project-info-box.box
        h1 #{project.project_name}
        p #{project.description}

      .project-tech-links-box.box
        h6 Technologies
        ul
          each tech in project.technologies
            li #{tech}

        a.btn-link(href=project.live_link target="_blank" rel="noopener noreferrer") Live Demo
        a.btn-link(href=project.github_link target="_blank" rel="noopener noreferrer") GitHub Repo

    .project-img-box.box
      each imgUrl in project.image_urls
        img.project-img(src=imgUrl, alt=`Screenshot of ${project.project_name}`)

  nav.nav
    a(href='/')
      span &larr;
      |  Back
